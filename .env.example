# Namuna. Vite qaysi faylni o'qishi rejimga bog'liq:
#   npm run dev    → .env.dev  (vite --mode dev) — proxy, same-origin
#   npm run build  → .env      (production)      — to'g'ridan-to'g'ri backend

# .env      (prod):
VITE_API_BASE_URL=https://api.falaqnashr.uz/api/v1

# .env.dev  (dev):
# VITE_API_BASE_URL=/api/v1
