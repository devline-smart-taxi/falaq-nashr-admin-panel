# Dev: Vite proxy (vite.config.ts) — brauzer uchun same-origin.
VITE_API_BASE_URL=/api/v1

# Proxy'siz to'g'ridan-to'g'ri ulanish (origin CORS ro'yxatida bo'lsa):
# VITE_API_BASE_URL=https://api.falaqnashr.uz/api/v1
