# DEV — `npm run dev` (vite --mode dev) shu faylni o'qiydi va .env ustidan yozadi.
# Backend CORS localhost'ga ruxsat bermaydi, shuning uchun Vite proxy orqali (same-origin).
VITE_API_BASE_URL=/api/v1
