# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env:
#   .env      → PROD (npm run build) — git'ga CHIQMAYDI, serverda qo'lda yaratiladi.
#   .env.dev  → DEV  (vite --mode dev) — commit qilinadi (zararsiz proxy config).
.env
.env.local
.env.*.local

# Deploy (nginx config + qo'llanma) — git'ga chiqmaydi, serverda qo'lda sozlanadi.
deploy/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
